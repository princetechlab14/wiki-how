<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= game?.title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-cyan-900 min-h-screen flex flex-col">
    <main class="flex-1 flex overflow-hidden">
        <!-- Center Banner -->
        <section class="flex-1 flex flex-col items-center justify-center relative">
            <!-- Banner Background -->
            <div class="absolute inset-0">
                <img src="<%= game?.logo %>" alt="Banner BG" class="w-full h-full object-cover opacity-10" />
                <div class="absolute inset-0 bg-black bg-opacity-30"></div>
            </div>
            <!-- Banner Content -->
            <div class="relative flex flex-col items-center z-10">
                <!-- Game Icon -->
                <div
                    class="rounded-full bg-yellow-200 shadow-xl flex items-center justify-center w-52 h-52 mb-8 border-4 border-white">
                    <img src="<%= game?.logo %>" alt="<%= game.title %>" class="w-52 h-52 rounded-full shadow-xl" />
                </div>
                <!-- Play Now Button -->
                <a href="<%= game?.game_url %>"
                    class="bg-gradient-to-b from-green-400 to-green-600 text-white font-bold text-xl px-12 py-3 rounded shadow-lg border-2 border-white mb-8 hover:from-green-500 hover:to-green-700 transition">
                    Play Now!
                </a>
                <!-- Game Title -->
                <div class="bg-black bg-opacity-20 px-8 py-2 rounded text-white font-semibold text-2xl shadow">
                    <%= game?.title %>
                </div>
            </div>
        </section>
    </main>
</body>

</html>