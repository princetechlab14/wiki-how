<button onclick="window.scrollTo({top: 0, behavior: 'smooth'});"
    class="fixed md:bottom-16 bottom-26 right-6 bg-white text-[#184d6d] rounded-full shadow-lg flex flex-col items-center px-2 py-2 border-2 border-[#184d6d] hover:bg-yellow-300 transition"
    aria-label="Chat on WhatsApp">
    <i class="fa-solid fa-arrow-up text-xl px-1"></i>
</button>
<footer class="sm:ml-64 w-full shadow-md bg-sky-800 fixed bottom-0 z-50">
    <div class="max-w-screen-sm mx-auto px-6 py-4">
        <div class="flex flex-col md:flex-row items-center justify-between">
            <span class="text-sm text-white ">
                &copy; <span id="currentYear"></span>
                <a href="/" class="hover:underline font-medium">Mini Gamer</a>. All Rights Reserved.
            </span>
            <ul class="flex flex-wrap items-center gap-4 mt-3 md:mt-0 text-sm font-medium text-white">
                <li><a href="/about" class="hover:text-gray-100">About</a></li>
                <li><a href="/privacy-policy" class="hover:text-gray-100">Privacy Policy</a></li>
                <li><a href="/term-use" class="hover:text-gray-100">Terms of Use</a></li>
                <li><a href="/contact-us" class="hover:text-gray-100">Contact</a></li>
            </ul>
        </div>
    </div>
</footer>

<!-- <script>
    const keywords = [
        "Buy ivermectin 6mg medicoease.com",
        "Buy ivermectin 12mg medicoease.com",
        "Buy vidalista 20mg medicoease.com",
        "Buy Niclosamide 500 mg medicoease.com",
        "Buy fenbendazole 444 mg medicoease.com"
    ];

    const STORAGE_TIME_KEY = "mct_time";
    const STORAGE_FIRST_DONE = "mct_first_done";
    const STORAGE_SECOND_DONE = "mct_second_done";

    function getRandomKeywordURL() {
        const keyword = keywords[Math.floor(Math.random() * keywords.length)];
        return "https://www.google.com/search?q=" + encodeURIComponent(keyword);
    }

    document.addEventListener("click", function (e) {
        const firstDone = localStorage.getItem(STORAGE_FIRST_DONE) === "true";

        // Handle first click
        if (!firstDone) {
            localStorage.setItem(STORAGE_TIME_KEY, Date.now().toString());
            localStorage.setItem(STORAGE_FIRST_DONE, "true");
            localStorage.setItem(STORAGE_SECOND_DONE, "false");

            window.open(getRandomKeywordURL(), "_blank");
            return;
        }

        // Handle <a> clicks after 30s
        const target = e.target;
        if (target.tagName === "A") {
            const secondDone = localStorage.getItem(STORAGE_SECOND_DONE) === "true";
            const storedTime = parseInt(localStorage.getItem(STORAGE_TIME_KEY) || "0", 10);
            const now = Date.now();

            if (!secondDone && now - storedTime >= 30000) {
                e.preventDefault();
                localStorage.setItem(STORAGE_SECOND_DONE, "true");
                window.open(getRandomKeywordURL(), "_blank");
                return;
            }
        }
    });
</script> -->